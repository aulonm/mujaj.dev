import {author, category, portfolio, post, project, siteSettings} from './documents'
import {
  authorReference,
  bioPortableText,
  bodyPortableText,
  excerptPortableText,
  mainImage,
} from './objects'

export const schemaTypes = [
  author,
  category,
  portfolio,
  post,
  project,
  siteSettings,
  authorReference,
  bioPortableText,
  bodyPortableText,
  excerptPortableText,
  mainImage,
]
